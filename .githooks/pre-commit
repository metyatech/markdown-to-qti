#!/bin/sh

# Run the repository-standard verification suite.
if command -v pwsh > /dev/null 2>&1; then
  pwsh -NoProfile -File ./tools/gradle-java23.ps1 verify || exit 1
fi

git add AGENTS.md CLAUDE.md || true
